{
  "name": "mbi-chronos",
  "private": true,
  "version": "1.75.0",
  "description": "MBI Chronos — backend (Supabase Edge Functions / Deno) + iOS. Domain logic canonical at backend/supabase/functions/_shared/domain.",
  "scripts": {
    "test:domain": "deno test backend/supabase/functions/_shared/domain/__tests__/",
    "test:integration": "deno test --allow-net --allow-env backend/supabase/functions/_tests_integration/",
    "check:structure": "bash scripts/check-structure.sh",
    "deploy:functions": "cd backend && supabase functions deploy --project-ref sjhysadnpswrcpmezmoc"
  }
}
